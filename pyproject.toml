[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "moka-news"
version = "0.1.0"
description = "Moka News - A TUI RSS news aggregator with AI summaries"
readme = "README.md"
requires-python = ">=3.8"
license = "MIT"
authors = [
    {name = "Gianluigi Calcaterra"}
]
keywords = ["rss", "news", "aggregator", "ai", "tui", "terminal", "openai", "anthropic", "gemini", "azure"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: End Users/Desktop",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Internet :: WWW/HTTP :: Dynamic Content :: News/Diary",
    "Topic :: Terminals",
    "Environment :: Console",
]
dependencies = [
    "feedparser>=6.0.10",
    "textual>=0.40.0",
    "openai>=1.0.0",
    "anthropic>=0.7.0",
    "python-dotenv>=1.0.0",
    "pyyaml>=6.0",
    "Pillow>=8.0.0",
    "qrcode>=7.3.0",
    "requests>=2.28.0",
]

[project.urls]
Homepage = "https://github.com/calca/moka-news"
Repository = "https://github.com/calca/moka-news"
Issues = "https://github.com/calca/moka-news/issues"
Changelog = "https://github.com/calca/moka-news/blob/main/CHANGELOG.md"

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
    "mypy>=1.14.1,<1.16.0",
    "types-requests>=2.31.0",
    "types-PyYAML>=6.0.12",
]
gemini = [
    "google-generativeai>=0.3.0",
]
mistral = [
    "mistralai>=0.0.7",
]
azure = [
    "azure-ai-inference>=1.0.0b9",
]
all = [
    "google-generativeai>=0.3.0",
    "mistralai>=0.0.7",
    "azure-ai-inference>=1.0.0b9",
]

[project.scripts]
moka-news = "moka_news.main:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["moka_news*"]

[tool.setuptools.package-data]
moka_news = [
    "templates/*.json",
    "fonts/*.ttf",
    "fonts/LICENSE",
    "py.typed",
]

[tool.black]
line-length = 88
target-version = ['py38']

[tool.ruff]
line-length = 88
target-version = "py38"

[tool.mypy]
python_version = "3.8"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false
ignore_missing_imports = true
files = [
    "moka_news/infrastructure/config",
    "moka_news/poster",
    "moka_news/publisher",
]
show_error_codes = true
